proxy:
  port:                    8080
  container-log-path:      /container-logs
  authentication:          simple
  heartbeat-rate:          10000
  heartbeat-timeout:       60000
  admin-groups:            admins
  users:
  - name:                  admin
    password:              edss123
    groups:                admins
  - name:                  guest
    password:              guest123
  docker:
      internal-networking: true
      bind-address:        127.0.0.1
      container-network:   "edss-net"
  specs:
  - id:                    01_testApp
    display-name:          Online Groundwater Model
    description:           Application which demonstrates the Shiny app coupling with Modflow program
    container-cmd:         ["R", "-e", "shiny::runApp('/root/shinyapp', host='0.0.0.0', port=3838)"]
    container-image:       test_2dmodel
    container-network:     "${proxy.docker.container-network}"
  - id:                    02_testApp
    display-name:          Crop Water Demand Calculator
    description:           Application which demonstrates the crop water model in a dashboard layout
    container-cmd:         ["R", "-e", "shiny::runApp('/root/shinyapp', host='0.0.0.0', port=3838)"]
    container-image:       test_ic
    container-volumes:     ["/d/users/YuLi/Dropbox/Personal_Workspace/Workspace/03Small_Projects/15EDSS_dev_framework/database/test_db.sqlite:/root/shinyapp/R_data/module_db/test_db.sqlite" ]
    container-network:     "${proxy.docker.container-network}"
  - id:                    03_testApp
    display-name:          airGR
    description:           Application which demonstrates the crop water model in a dashboard layout
    container-cmd:         ["R", "-e", "shiny::runApp('/root/shinyapp', host='0.0.0.0', port=3838)"]
    container-image:       airgr_app
    container-network:     "${proxy.docker.container-network}"

logging:
  file:
    /log/shinyproxy.log
